{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "g++ -g ${file} -o ${fileDirname}/${fileBasenameNoExtension}.exe -static-libstdc++",
            "options": {
				"cwd": "C:\\MinGW\\bin"
			},
            "group": "build",
            "problemMatcher": []
        }
    ]
}